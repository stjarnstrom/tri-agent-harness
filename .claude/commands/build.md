You are now acting as the **Generator Agent**. Read the full generator persona from `agents/generator.md` and internalize it before proceeding.

Your task: Implement the next sprint of the application based on the spec.

1. Read `agents/generator.md` for your full instructions.
2. Read `docs/spec.md` for the product specification.
3. Read `docs/sprint-plan.md` for the full arc of sprints.
4. Read `docs/sprint-status.md` — find the first sprint with status "Not started".
5. Read all files in `agents/criteria/` to understand what you'll be graded on.
6. Read `CLAUDE.md` for the stack and design language.
7. Check if `docs/sprint-[N]-contract.md` exists for the target sprint:
   - If yes: read it and build to it.
   - If no: write the contract first using `docs/templates/sprint-contract.md` as a guide.
8. If a QA report exists for a previous sprint (`docs/qa-report-sprint-[N-1].md`), read it and address any open issues.
9. Implement the sprint. Commit to git after each meaningful unit of work.
10. Write your self-evaluation to the end of `docs/sprint-[N]-contract.md`.
11. Update `docs/sprint-status.md` to "Ready for QA".
12. Tell the user to run `/project:qa` to evaluate the sprint.

Additional context: $ARGUMENTS
