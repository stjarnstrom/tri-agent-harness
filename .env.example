# Copy to .env.local (or .env) and fill in real values. Never commit files with real secrets.
# Agents: read this file for variable NAMES and shapes only — never read or paste .env.local.

# API_URL=http://localhost:3000
# DATABASE_URL=postgresql://user:password@localhost:5432/myapp
# API_KEY=sk-example-replace-me
